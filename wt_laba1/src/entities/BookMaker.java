package entities;

public class BookMaker extends Human {
	
	public void SetCoefficients(Race race)
	{
		for (int i = 0; i < Integer.parseInt(race.getAnimalsCount()); i++ )
		{
			//race.coefficients.add(String.valueOf(Math.random()*10));
			
		}
	}

}
